import type { ComponentMeta } from '@storybook/react-native';
import { Icon } from './Icon';
const MyIconMeta: ComponentMeta<typeof Icon> = {
  title: 'api/stories/Icon',
  component: Icon,
};

export { Icon } from './Icon';
export default MyIconMeta;
